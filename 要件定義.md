# 講座プラットフォーム MVP 要件定義書

## 1. プロジェクト概要

YouTubeで公開している動画を活用した、Udemy風のオンライン講座プラットフォームを構築する。
まずはMVPとして最低限の機能を実装し、後から課金機能（Stripe）を追加できる設計とする。

### ターゲットユーザー

- AIを使って開発したいエンジニア
- AIを使って開発したい非エンジニア

---

## 2. 技術スタック

| 項目                         | 技術                               |
| ---------------------------- | ---------------------------------- |
| フロントエンド・バックエンド | Next.js (App Router)               |
| データベース                 | Supabase (PostgreSQL)              |
| 認証                         | Supabase Auth（Google OAuth のみ） |
| 動画ホスティング             | YouTube 埋め込み                   |
| デプロイ                     | Vercel                             |

---

## 3. 講座構成（3階層）

```
講座（Course）
└── セクション（Section）
    └── 動画（Lesson）
```

- 1講座あたり約30動画
- 複数講座に対応
- 各動画はYouTube URLで管理し、iframe埋め込みで表示

---

## 4. アクセス制御

| 状態                     | アクセス範囲                            |
| ------------------------ | --------------------------------------- |
| 未ログイン               | 各講座の**第1動画のみ**視聴可能（試聴） |
| ログイン済み（権限なし） | 第1動画のみ視聴可能                     |
| ログイン済み（権限あり） | 該当講座の**全動画**視聴可能            |

- 権限は管理者が管理画面から手動で付与する
- 講座ごとに個別に権限を管理する（講座Aの権限を持っていても講座Bは見られない）

---

## 5. 進捗管理

- 動画ごとに「視聴済み」チェックをユーザーが**手動でマーク**できる
- MVP時点では進捗バー・修了証・続きから再生は対象外（後回し）

---

## 6. 画面一覧

### ユーザー向け画面

| 画面名            | パス                                 | 概要                                                   |
| ----------------- | ------------------------------------ | ------------------------------------------------------ |
| トップ / 講座一覧 | `/`                                  | 公開中の講座カード一覧                                 |
| 講座詳細          | `/courses/[slug]`                    | 講座説明・カリキュラム一覧・受講開始ボタン             |
| 動画視聴          | `/courses/[slug]/lessons/[lessonId]` | YouTube埋め込み・セクション/動画一覧・視聴済みチェック |
| ログイン          | `/login`                             | Googleアカウントでログイン（OAuth）                    |

### 管理者向け画面

| 画面名               | パス                       | 概要                                  |
| -------------------- | -------------------------- | ------------------------------------- |
| 管理トップ           | `/admin`                   | 管理メニュー                          |
| 講座管理             | `/admin/courses`           | 講座のCRUD                            |
| セクション・動画管理 | `/admin/courses/[id]/edit` | セクション・動画のCRUD、順序変更      |
| ユーザー権限管理     | `/admin/users`             | ユーザー一覧・講座ごとの権限付与/剥奪 |

---

## 7. データベース設計（Supabase）

### `courses`（講座）

```sql
id          uuid primary key
title       text
slug        text unique
description text
thumbnail_url text
is_published boolean default false
created_at  timestamp
```

### `sections`（セクション）

```sql
id          uuid primary key
course_id   uuid references courses(id)
title       text
order       int
created_at  timestamp
```

### `lessons`（動画）

```sql
id            uuid primary key
section_id    uuid references sections(id)
title         text
youtube_url   text
order         int
is_free       boolean default false  -- trueなら未ログインでも視聴可
created_at    timestamp
```

### `user_course_access`（受講権限）

```sql
id          uuid primary key
user_id     uuid references auth.users(id)
course_id   uuid references courses(id)
granted_at  timestamp
```

### `lesson_progress`（視聴進捗）

```sql
id          uuid primary key
user_id     uuid references auth.users(id)
lesson_id   uuid references lessons(id)
watched_at  timestamp
unique(user_id, lesson_id)
```

---

## 8. MVPスコープ（実装する / しない）

### ✅ 実装する（MVP）

- [ ] Supabase 認証（Google OAuth によるログイン・ログアウト）
- [ ] 講座一覧ページ
- [ ] 講座詳細ページ（カリキュラム表示）
- [ ] 動画視聴ページ（YouTube埋め込み）
- [ ] アクセス制御（未ログイン / ログイン+権限あり）
- [ ] 視聴済み手動チェック機能
- [ ] 管理画面（講座・セクション・動画のCRUD）
- [ ] 管理画面（ユーザーへの権限付与）

### ❌ 実装しない（後回し）

- Stripe 課金
- 進捗バー・パーセンテージ表示
- 修了証の発行
- 続きから再生（最後に見た動画への自動遷移）
- コメント・Q&A機能
- メール通知

---

## 9. 将来的な拡張（Phase 2以降）

- Stripe連携による有料販売（買い切り or サブスク）
- 進捗バー・修了証
- 続きから再生
- コース検索・フィルター
- レビュー・評価機能

---

## 10. 非機能要件

- 管理画面は `/admin` 以下に集約し、Supabase の `role` カラムで管理者を識別して保護する
- YouTube埋め込みは `youtube-nocookie.com` ドメインを使用してプライバシーに配慮する
- モバイル対応（レスポンシブデザイン）
